# === openenv.yaml ===

environment_id: traffic-med-env
version: "1.0"

entry_point: env:TrafficMedEnv

tasks:
  easy:
    description: "Normal traffic conditions"
    difficulty: easy

  medium:
    description: "Moderate traffic with ambulance priority"
    difficulty: medium

  hard:
    description: "Heavy traffic with high ambulance severity"
    difficulty: hard

metadata:
  author: "NayakalKarthik2026"
  description: "Traffic signal control with ambulance prioritization using reinforcement learning"
